# Engine

SET ( FILES coordinatechooser coordinates pythonmanager reginaabout reginaprefset shortrunner )

IF(PYTHONLIBS_FOUND)
  ADD_SUBDIRECTORY(python)
ENDIF(PYTHONLIBS_FOUND)

FOREACH( FILE ${FILES} )
  SET( COMMON_SOURCES ${COMMON_SOURCES} ${CMAKE_CURRENT_SOURCE_DIR}/${FILE})
ENDFOREACH(FILE)
SET( COMMON_SOURCES ${COMMON_SOURCES} PARENT_SCOPE)
SET( MOC_HEADERS ${MOC_HEADERS} ${CMAKE_CURRENT_SOURCE_DIR}/coordinatechooser.h ${CMAKE_CURRENT_SOURCE_DIR}/shortrunner.h)
SET( MOC_HEADERS ${MOC_HEADERS} PARENT_SCOPE )
