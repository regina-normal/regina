User Interface Helper Tools
---------------------------

This directory provides common tools for user interfaces that wish to embed
python interpreter(s) with regina-python running inside.

The source files in this directory should be built directly into the
each user interface that uses them.  They are *not* built into Regina's
python module.
